#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{
    int p;
    scanf("%d", &p);
    if (p == 1)
        printf("Bulbassauro");
    else if (p == 2)
        printf("Charmander");
    else if (p == 3)
        printf("Squirtle");
    system("pause");
    return 0;
}