#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{
    int nascimento, atual;
    scanf("%d%d", &nascimento, &atual);
    printf("%d", (atual-nascimento));
	return 0;
}