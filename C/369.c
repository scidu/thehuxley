#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{
    int d;
    float m;
    scanf("%d", &d);
    m = (d * 0.75);
    printf("%.2f", m);
    system("pause");
    return 0;
}